# Project Instructions

## IMPORTANT: No Direct Commits

Never commit or push changes directly to this repository.

All file edits must be proposed as diffs or instructions only — the user will apply them via Docker so that commits are attributed to the repository owner, not Claude.

Do not run `git commit`, `git push`, or any git write commands (force-push, rebase, reset, etc.) on this repo.
